name: Desplegar web

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write
  issues: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Regenerar el sitio
        run: npm run build

      - name: Ensamblar carpeta pÃºblica
        run: |
          mkdir -p dist
          cp index.html 404.html privacidad.html fotos.html directo.html styles.css app.js nav.js dist/
          cp CNAME dist/ 2>/dev/null || true
          cp -r fonts dist/
          cp -r images dist/
          cp -r fotos dist/
          cp -r directo dist/
          cp -r episodios dist/
          cp -r data dist/
          cp -r etiquetas dist/
          cp directo.json dist/
          cp sitemap.xml robots.txt dist/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

  verify:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Esperar propagaciÃ³n CDN
        run: sleep 30

      - name: Verificar web actualizada
        run: |
          HTTP=$(curl -s -o /dev/null -w "%{http_code}" https://bienvenidoalos90.com/fotos.html)
          if [ "$HTTP" != "200" ]; then
            echo "ERROR: fotos.html devolviÃ³ HTTP $HTTP"
            exit 1
          fi
          echo "âœ… Web accesible (HTTP 200)"

  notify-on-failure:
    needs: [build, deploy, verify]
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const title = "âš ï¸ FallÃ³ el workflow 'Desplegar web'";
            const body = `La ejecuciÃ³n [${context.runId}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) ha fallado. Revisa los logs.`;
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open",
              labels: "workflow-failure",
            });
            const existing = issues.find((i) => i.title === title);
            if (existing) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.number,
                body,
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: ["workflow-failure"],
              });
            }
